name: Deploy Docs to GitHub Pages
on:
  push:
    branches: [ main ]
    paths: [ 'docs/**' ]
jobs:
  pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "pages placeholder"
